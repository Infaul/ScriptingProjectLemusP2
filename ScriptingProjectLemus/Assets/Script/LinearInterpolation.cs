using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinearInterpolation : MonoBehaviour
{/*
    //In this case, result = 4, the Mathf.Lerp function takes 3 float perameters to interpolate from . since 0.5 is the third, the interpolation value will be 50%
    float result = Mathf.Lerp (3f, 5f, 0.5f);

    Vector 3 from = new Vector3 ( 1f, 2f, 3f)
    Vector3 to = new Vector3 (5f, 6f, 7f)

    //Here result = (4,5,6), an example of a lerp function; takes 3 parameters with the third number determining the amount to interpolate to get a percentage of the way of a vector
    Vector3 result = Vector3.Lerp (from, to, 0.75f);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
      // a lerp function used to determine the intensity of light; over several frames the lights intensity will go to 8 but slows down as it does
        light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f);
    


    // a lerp function similar to the one above, the only difference being that the change in light happens per second instead of per frame
    light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f * Time.deltaTime);
    }
    */
}
